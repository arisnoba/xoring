---
phase: 02-static-section-reveal
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/PocSection.tsx
autonomous: true
requirements:
  - SECT-03
must_haves:
  truths:
    - "사용자는 Poc 상단 블록이 카피와 대시보드 이미지 순서로 reveal 되는 것을 본다."
    - "사용자는 Poc 중단 블록에서 프로세스 카피, steps 그룹, 하단 카드 그룹이 독립적으로 reveal 되는 것을 본다."
    - "사용자는 Poc 하단 블록에서 앱 안내 카피와 폰 이미지가 별도 reveal 되는 것을 본다."
  artifacts:
    - path: src/components/sections/PocSection.tsx
      provides: "상단/중단/하단 3블록 reveal 구조"
  key_links:
    - from: src/components/sections/PocSection.tsx
      to: src/components/shared/RevealOnScroll.tsx
      via: "세 블록 wrapper"
      pattern: "RevealOnScroll"
    - from: src/components/sections/PocSection.tsx
      to: src/lib/motion.ts
      via: "fadeUp/scaleUp preset import"
      pattern: "fadeUp|scaleUp"
---

<objective>
`PocSection`을 상단/중단/하단 3개 블록 단위로 분리해 독립 reveal 흐름을 만든다.

Purpose: SECT-03의 핵심 리스크 섹션에서 중복 DOM과 모바일/데스크톱 분기 구조를 유지하면서 블록 단위 reveal 원칙을 구현한다.
Output: Poc 섹션이 3개의 독립 reveal 트리거를 갖고 각 블록 내부 delay 값이 문서와 일치한다.
Prerequisite: Phase 1의 `01-01-PLAN.md`가 완료되어 `RevealOnScroll`과 `src/lib/motion.ts` 프리셋이 이미 존재하는 브랜치에서 실행한다.
</objective>

<execution_context>
@/Users/arisnoba/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arisnoba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-motion-foundation/01-01-PLAN.md
@.planning/phases/01-motion-foundation/01-CONTEXT.md
@docs/motion-plan.md
@src/components/sections/PocSection.tsx
@src/components/shared/SectionBackground.tsx
@src/components/shared/SectionContainer.tsx

Phase prerequisite: 이 plan은 `01-01-PLAN.md`가 먼저 적용되어 `src/components/shared/RevealOnScroll.tsx`와 `src/lib/motion.ts`가 이미 생성된 상태를 전제로 한다.

<interfaces>
`src/components/shared/SectionBackground.tsx`
```tsx
interface SectionBackgroundProps {
  desktopSrc: string;
  mobileSrc?: string;
  opacity?: number;
  className?: string;
}
```

Required Poc reveal groups from `docs/motion-plan.md`
```tsx
// block 1
<RevealOnScroll variants={fadeUp}>badge/title/subcopy</RevealOnScroll>
<RevealOnScroll variants={scaleUp} delay={0.2}>dashboard image</RevealOnScroll>

// block 2
<RevealOnScroll variants={fadeUp}>quote</RevealOnScroll>
<RevealOnScroll variants={fadeUp} delay={0.15}>steps group</RevealOnScroll>
<RevealOnScroll variants={fadeUp} delay={0.25}>bottom cards group</RevealOnScroll>

// block 3
<RevealOnScroll variants={fadeUp}>closing copy</RevealOnScroll>
<RevealOnScroll variants={scaleUp} delay={0.15}>phone image</RevealOnScroll>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Poc 상단/중단/하단 3블록 reveal 구조를 구현</name>
  <files>src/components/sections/PocSection.tsx</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-motion-foundation/01-01-PLAN.md
.planning/phases/01-motion-foundation/01-CONTEXT.md
docs/motion-plan.md
src/components/sections/PocSection.tsx
  </read_first>
  <action>`PocSection.tsx`에 `RevealOnScroll`, `fadeUp`, `scaleUp` import를 추가하고 파일은 서버 컴포넌트로 유지한다. 첫 번째 루트 `<div>` 내부에서 상단 텍스트 그룹(배지/타이틀/서브카피)은 `fadeUp` 기반 개별 wrapper 3개로 분리하고, 대시보드 이미지 컨테이너는 `RevealOnScroll variants={scaleUp} delay={0.2}`로 감싼다. 두 번째 루트 `<div>` 내부에서는 인용 카피를 `RevealOnScroll variants={fadeUp}`, desktop/mobile steps 두 레이아웃을 감싸는 공통 부모를 `RevealOnScroll variants={fadeUp} delay={0.15}`, 하단 카드 3장을 감싸는 flex 컨테이너를 `RevealOnScroll variants={fadeUp} delay={0.25}`로 감싼다. 마지막 하단 `<div className="mt-20 text-center">`에서는 안내 카피를 `fadeUp`, 폰 이미지 wrapper를 `scaleUp delay={0.15}`로 분리한다. `ChevronRight`, desktop/mobile DOM 분기, 이미지 `unoptimized`, 카드 className, `SectionBackground`는 그대로 유지하고 `HeroSection`, `TwoModesSection`, `ModeCardCarousel`은 수정하지 않는다.</action>
  <verify>
    <automated>rg -n "RevealOnScroll|fadeUp|scaleUp|0\\.2|0\\.15|0\\.25" src/components/sections/PocSection.tsx</automated>
  </verify>
  <acceptance_criteria>
- `src/components/sections/PocSection.tsx`에 `RevealOnScroll`, `fadeUp`, `scaleUp` import가 존재한다.
- 상단 배지/타이틀/서브카피와 대시보드 이미지가 서로 다른 `RevealOnScroll` wrapper로 분리된다.
- steps용 desktop `md:flex` 블록과 mobile `md:hidden` 블록을 함께 감싸는 상위 `RevealOnScroll variants={fadeUp} delay={0.15}`가 존재한다.
- 하단 카드 flex 컨테이너가 `RevealOnScroll variants={fadeUp} delay={0.25}` 내부에 있다.
- 하단 폰 이미지 wrapper가 `RevealOnScroll variants={scaleUp} delay={0.15}`를 사용한다.
- `src/components/sections/PocSection.tsx`에 `'use client'`가 추가되지 않는다.
  </acceptance_criteria>
  <done>Poc 섹션은 상단/중단/하단 3개 블록이 독립적으로 reveal 되며 desktop/mobile steps DOM 분기를 그대로 유지한다.</done>
</task>

<task type="auto">
  <name>Task 2: Poc 블록 경계와 brownfield 비침범 여부를 코드 수준에서 점검</name>
  <files>src/components/sections/PocSection.tsx</files>
  <read_first>
.planning/PROJECT.md
.planning/phases/01-motion-foundation/01-01-PLAN.md
.planning/phases/01-motion-foundation/01-CONTEXT.md
src/components/sections/PocSection.tsx
src/components/sections/HeroSection.tsx
src/components/sections/TwoModesSection.tsx
src/components/shared/ModeCardCarousel.tsx
  </read_first>
  <action>Task 1 완료 후 `PocSection.tsx`의 허용 wrapper 위치를 정확히 고정한다. 허용되는 `RevealOnScroll`은 총 9개뿐이며 위치는 1) 상단 배지, 2) 상단 타이틀, 3) 상단 서브카피, 4) 상단 대시보드 이미지 컨테이너, 5) 중단 인용 카피, 6) steps desktop+mobile 공통 부모, 7) bottom cards 외부 flex 컨테이너, 8) 하단 안내 카피, 9) 하단 폰 이미지 컨테이너다. `steps.map(...)` 콜백 내부와 `bottomCards.map(...)` 콜백 내부에는 `RevealOnScroll`을 추가하지 않는다. 즉 `React.Fragment key={step.label}` 바로 안쪽 요소들, `ChevronRight`, `<article key={card.title}>`는 기존 구조를 유지하고 직접 reveal wrapper로 감싸지 않는다. `HeroSection.tsx`, `TwoModesSection.tsx`, `ModeCardCarousel.tsx`는 수정 대상이 아니며 이 plan에서 변경하지 않는다.</action>
  <verify>
    <automated>[ "$(rg -c "<RevealOnScroll" src/components/sections/PocSection.tsx)" = "9" ] && ! perl -0ne 'exit 1 if /steps\\.map\\([^)]*=>\\s*\\(\\s*<RevealOnScroll/s; exit 1 if /bottomCards\\.map\\([^)]*=>\\s*\\(\\s*<RevealOnScroll/s; END { exit 0 }' src/components/sections/PocSection.tsx && ! rg -n "'use client'" src/components/sections/PocSection.tsx && ! git diff --quiet -- src/components/sections/HeroSection.tsx src/components/sections/TwoModesSection.tsx src/components/shared/ModeCardCarousel.tsx</automated>
  </verify>
  <acceptance_criteria>
- `src/components/sections/PocSection.tsx`의 `<RevealOnScroll` 문자열 개수가 정확히 9개다.
- `steps.map(...)` 콜백 시작 직후에 `<RevealOnScroll`이 오지 않는다.
- `bottomCards.map(...)` 콜백 시작 직후에 `<RevealOnScroll`이 오지 않는다.
- `src/components/sections/PocSection.tsx`에 `'use client'`가 없다.
- `src/components/sections/HeroSection.tsx`, `src/components/sections/TwoModesSection.tsx`, `src/components/shared/ModeCardCarousel.tsx`는 이 plan 실행으로 변경되지 않는다.
  </acceptance_criteria>
  <done>Poc reveal 구현이 블록 단위 원칙을 지키고 Phase 2 범위 밖 brownfield 핵심 파일을 침범하지 않는다.</done>
</task>

</tasks>

<verification>
`rg -n "RevealOnScroll|fadeUp|scaleUp" src/components/sections/PocSection.tsx`로 wrapper 위치를 확인하고, `PocSection.tsx`의 `<RevealOnScroll` 개수가 9개인지와 `steps.map(...)`/`bottomCards.map(...)` 콜백 내부에 직접 wrapper가 없는지를 확인한다.
</verification>

<success_criteria>
`PocSection.tsx`가 3개 독립 블록 reveal 구조를 가지며, reveal 추가 때문에 Phase 2 제외 대상 파일이 수정되지 않는다.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-section-reveal/02-03-SUMMARY.md`
</output>
