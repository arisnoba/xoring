---
phase: 02-static-section-reveal
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/AIAgentSection.tsx
  - src/components/sections/ApplicationSection.tsx
autonomous: true
requirements:
  - SECT-03
must_haves:
  truths:
    - "사용자는 AIAgent 섹션에서 헤더 카피와 4개 카드가 순차 reveal 되는 것을 본다."
    - "사용자는 Application 섹션에서 폰 이미지와 CTA 블록이 분리된 reveal로 들어오는 것을 본다."
  artifacts:
    - path: src/components/sections/AIAgentSection.tsx
      provides: "헤더와 카드 stagger reveal"
    - path: src/components/sections/ApplicationSection.tsx
      provides: "폰/텍스트/다운로드 CTA reveal"
  key_links:
    - from: src/components/sections/AIAgentSection.tsx
      to: src/components/shared/RevealOnScroll.tsx
      via: "badge/headline/subtext/card wrapper"
      pattern: "RevealOnScroll"
    - from: src/components/sections/ApplicationSection.tsx
      to: src/components/shared/StoreButtons.tsx
      via: "CTA wrapper 유지"
      pattern: "StoreButtons"
    - from: src/components/sections/ApplicationSection.tsx
      to: src/lib/motion.ts
      via: "scaleUp/fadeUp/fadeIn preset import"
      pattern: "scaleUp|fadeUp|fadeIn"
---

<objective>
블록형 정적 섹션인 `AIAgent`와 `Application`에 계획된 stagger reveal을 적용한다.

Purpose: SECT-03 중 상대적으로 구조가 단순한 카드/CTA 섹션에서 블록 단위 reveal 패턴을 확정한다.
Output: AIAgent 카드 4장과 Application 폰/카피/다운로드 영역이 공통 프리셋 기반 reveal 구조를 가진다.
Prerequisite: Phase 1의 `01-01-PLAN.md`가 완료되어 `RevealOnScroll`과 `src/lib/motion.ts` 프리셋이 이미 존재하는 브랜치에서 실행한다.
</objective>

<execution_context>
@/Users/arisnoba/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arisnoba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-motion-foundation/01-01-PLAN.md
@.planning/phases/01-motion-foundation/01-CONTEXT.md
@docs/motion-plan.md
@src/components/sections/AIAgentSection.tsx
@src/components/sections/ApplicationSection.tsx
@src/components/shared/StoreButtons.tsx
@src/components/shared/SectionContainer.tsx

Phase prerequisite: 이 plan은 `01-01-PLAN.md`가 먼저 적용되어 `src/components/shared/RevealOnScroll.tsx`와 `src/lib/motion.ts`가 이미 생성된 상태를 전제로 한다.

<interfaces>
`src/components/shared/StoreButtons.tsx`
```tsx
interface StoreButtonsProps {
  className?: string;
  variant?: 'dark' | 'light';
  googleFirst?: boolean;
  buttonClassName?: string;
}
```

Phase 1 contract from `docs/motion-plan.md`
```tsx
<RevealOnScroll variants={scaleUp} delay={0.1 * index}>...</RevealOnScroll>
<RevealOnScroll variants={fadeUp} delay={0.3}>...</RevealOnScroll>
<RevealOnScroll variants={fadeIn} delay={0.35}>...</RevealOnScroll>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: AIAgent 헤더와 4개 카드에 wave reveal 적용</name>
  <files>src/components/sections/AIAgentSection.tsx</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-motion-foundation/01-01-PLAN.md
.planning/phases/01-motion-foundation/01-CONTEXT.md
docs/motion-plan.md
src/components/sections/AIAgentSection.tsx
  </read_first>
  <action>`AIAgentSection.tsx`에 `RevealOnScroll`, `fadeUp`, `scaleUp` import를 추가하고 `'use client'`는 추가하지 않는다. 상단 텍스트 영역에서는 `AI AGENT` 배지에 `RevealOnScroll variants={fadeUp}`, 제목에 `delay={0.1}`, 서브카피에 `delay={0.15}`를 적용한다. 카드 grid에서는 `agents.map((agent, index) => ...)`로 바꿔 각 카드 루트를 `RevealOnScroll variants={scaleUp} delay={0.1 * index}`로 감싼다. 카드 내부의 제목, 이미지, 설명 DOM 구조와 기존 `Image` props, 그림자 className은 유지하고 카드 간격과 레이아웃 breakpoints는 바꾸지 않는다.</action>
  <verify>
    <automated>rg -n "RevealOnScroll|fadeUp|scaleUp|0\\.1 \\* index|delay=\\{0\\.15\\}" src/components/sections/AIAgentSection.tsx</automated>
  </verify>
  <acceptance_criteria>
- `src/components/sections/AIAgentSection.tsx`에 `RevealOnScroll`, `fadeUp`, `scaleUp` import가 존재한다.
- 배지, 제목, 서브카피 각각이 개별 `RevealOnScroll` wrapper를 가진다.
- 카드 map 함수가 `index`를 받고 카드 루트에 `delay={0.1 * index}`가 들어간 `RevealOnScroll variants={scaleUp}`를 사용한다.
- `src/components/sections/AIAgentSection.tsx`에 `'use client'`가 추가되지 않는다.
  </acceptance_criteria>
  <done>AIAgent 섹션은 헤더 3단과 카드 4장이 왼쪽에서 오른쪽으로 순차 reveal 되며 기존 카드 레이아웃을 유지한다.</done>
</task>

<task type="auto">
  <name>Task 2: Application 폰 이미지와 CTA 블록을 분리 reveal로 배치</name>
  <files>src/components/sections/ApplicationSection.tsx</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-motion-foundation/01-01-PLAN.md
.planning/phases/01-motion-foundation/01-CONTEXT.md
docs/motion-plan.md
src/components/sections/ApplicationSection.tsx
src/components/shared/StoreButtons.tsx
  </read_first>
  <action>`ApplicationSection.tsx`에 `RevealOnScroll`, `scaleUp`, `fadeUp`, `fadeIn` import를 추가한다. 왼쪽 폰 이미지 wrapper는 `RevealOnScroll variants={scaleUp}`로 감싸고, 오른쪽 텍스트 컬럼에서는 배지 `delay={0.1}`, 제목 `delay={0.15}`, 서브카피 `delay={0.2}`, `StoreButtons` wrapper `delay={0.3}`의 `fadeUp`을 적용한다. 하단 안내문 `* Some features ...`는 `RevealOnScroll variants={fadeIn} delay={0.35}`로 분리한다. 기존 `StoreButtons` props, gradient overlay, grid className, section root className은 유지하고 파일을 클라이언트 컴포넌트로 승격하지 않는다.</action>
  <verify>
    <automated>rg -n "RevealOnScroll|scaleUp|fadeUp|fadeIn|0\\.35|StoreButtons" src/components/sections/ApplicationSection.tsx</automated>
  </verify>
  <acceptance_criteria>
- `src/components/sections/ApplicationSection.tsx`에 `RevealOnScroll`, `scaleUp`, `fadeUp`, `fadeIn` import가 존재한다.
- 폰 이미지 wrapper가 `RevealOnScroll variants={scaleUp}`를 사용한다.
- 배지/제목/서브카피/StoreButtons/안내문이 각각 계획된 delay 값의 wrapper를 가진다.
- `StoreButtons`의 `variant="light"`와 `googleFirst` prop이 유지된다.
- `src/components/sections/ApplicationSection.tsx`에 `'use client'`가 추가되지 않는다.
  </acceptance_criteria>
  <done>Application 섹션은 폰 이미지와 텍스트/CTA가 분리된 reveal 흐름을 가지며 기존 다운로드 버튼 동작과 레이아웃을 유지한다.</done>
</task>

</tasks>

<verification>
`rg -n "RevealOnScroll|fadeUp|fadeIn|scaleUp" src/components/sections/AIAgentSection.tsx src/components/sections/ApplicationSection.tsx`로 wrapper/preset 배치를 확인한다.
</verification>

<success_criteria>
`AIAgent`와 `Application` 두 파일이 서버 컴포넌트 상태를 유지한 채 계획 문서의 delay/preset 값을 반영하고, 카드/CTA DOM 구조를 깨지 않는다.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-section-reveal/02-02-SUMMARY.md`
</output>
