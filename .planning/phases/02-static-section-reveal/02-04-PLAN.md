---
phase: 02-static-section-reveal
plan: 4
type: execute
wave: 2
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - .planning/phases/02-static-section-reveal/02-04-SUMMARY.md
autonomous: false
requirements:
  - SECT-01
  - SECT-03
must_haves:
  truths:
    - "정적 섹션 reveal 적용 후에도 `npm run lint`가 통과한다."
    - "정적 export 기준의 `npm run build`가 통과한다."
    - "사용자는 데스크톱과 모바일에서 BeyondScreen, AIAgent, Application, Poc, Web3, Pioneer reveal이 계획대로 보인다고 확인한다."
  artifacts:
    - path: .planning/phases/02-static-section-reveal/02-04-SUMMARY.md
      provides: "자동/수동 검증 결과 기록"
  key_links:
    - from: npm run build
      to: src/app/page.tsx
      via: "전체 섹션 조립"
      pattern: "Home"
    - from: manual viewport check
      to: src/components/sections/PocSection.tsx
      via: "상단/중단/하단 블록 reveal"
      pattern: "The Value of Sweat"
---

<objective>
Phase 2에서 적용한 정적 섹션 reveal을 자동 검증하고, 데스크톱/모바일에서 사용자 확인까지 마무리한다.

Purpose: brownfield 제약을 깨지 않았는지 릴리스 전 수준으로 확인하고, SECT-01/SECT-03의 실제 화면 결과를 잠근다.
Output: lint/build 결과와 수동 검증 결과가 Phase 2 summary에 기록된다.
</objective>

<execution_context>
@/Users/arisnoba/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arisnoba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@src/app/page.tsx
@src/components/sections/BeyondScreenSection.tsx
@src/components/sections/AIAgentSection.tsx
@src/components/sections/ApplicationSection.tsx
@src/components/sections/PocSection.tsx
@src/components/sections/Web3Section.tsx
@src/components/sections/PioneerSection.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Phase 2 reveal 자동 검증 실행</name>
  <files>.planning/phases/02-static-section-reveal/02-04-SUMMARY.md</files>
  <read_first>
.planning/PROJECT.md
.planning/ROADMAP.md
src/app/page.tsx
src/components/sections/BeyondScreenSection.tsx
src/components/sections/AIAgentSection.tsx
src/components/sections/ApplicationSection.tsx
src/components/sections/PocSection.tsx
src/components/sections/Web3Section.tsx
src/components/sections/PioneerSection.tsx
  </read_first>
  <action>Wave 1 plan이 모두 끝난 뒤 루트에서 `npm run lint`와 `npm run build`를 순서대로 실행한다. 실행 결과는 `.planning/phases/02-static-section-reveal/02-04-SUMMARY.md`에 반드시 기록한다. 실패 시 실패한 명령, 에러가 난 섹션 파일, 재수정이 필요한 원래 plan 번호를 summary에 적고, 성공 시에는 두 명령의 통과 여부와 정적 export build 완료 사실을 summary에 적는다.</action>
  <verify>
    <automated>npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
- `npm run lint`가 exit code 0으로 끝난다.
- `npm run build`가 exit code 0으로 끝난다.
- 실패 시 summary에 실패 명령과 원인 파일이 기록된다.
  </acceptance_criteria>
  <done>Phase 2 적용 이후 lint/build 상태가 명확히 기록되고, 정적 export 빌드가 통과하거나 실패 원인이 특정된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 데스크톱/모바일 reveal 흐름 수동 확인</name>
  <files>.planning/phases/02-static-section-reveal/02-04-SUMMARY.md</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
docs/motion-plan.md
src/app/page.tsx
src/components/sections/BeyondScreenSection.tsx
src/components/sections/AIAgentSection.tsx
src/components/sections/ApplicationSection.tsx
src/components/sections/PocSection.tsx
src/components/sections/Web3Section.tsx
src/components/sections/PioneerSection.tsx
  </read_first>
  <action>Task 1의 `npm run lint && npm run build`가 통과한 뒤에만 수동 검증으로 넘어간다. 실행자는 `npm run dev`로 로컬 서버를 띄우고, 사용자가 데스크톱과 모바일에서 6개 대상 섹션 reveal을 확인할 수 있도록 URL과 체크 포인트를 전달한다. 사용자 응답은 `.planning/phases/02-static-section-reveal/02-04-SUMMARY.md`에 반드시 기록한다. 승인 전에는 추가 코드 수정이나 다른 phase 실행으로 넘어가지 않는다.</action>
  <verify>
    <automated>MISSING — checkpoint task; automated validation is completed by Task 1 before requesting user approval</automated>
  </verify>
  <acceptance_criteria>
- 사용자가 데스크톱과 모바일 기준으로 최소 6개 섹션(BeyondScreen, AIAgent, Application, Poc, Web3, Pioneer)의 reveal 결과를 확인한다.
- 사용자가 `approved` 또는 구체적인 이슈 목록으로 응답한다.
- `Poc` 상단/중단/하단 블록의 독립 reveal 여부가 응답에 포함된다.
  </acceptance_criteria>
  <what-built>정적 섹션 reveal 적용과 lint/build 자동 검증이 끝난 Phase 2 결과물</what-built>
  <how-to-verify>
1. `npm run dev`로 로컬 서버를 띄운 뒤 `/`를 연다.
2. 데스크톱 폭에서 `BeyondScreen`, `AIAgent`, `Application`, `Poc`, `Web3`, `Pioneer` 순서로 스크롤하며 reveal이 한 번씩만 재생되는지 확인한다.
3. `Poc`에서는 상단(타이틀+대시보드), 중단(If a computer... + steps + 하단 카드), 하단(Prove your value... + 폰 이미지)이 각자 독립적으로 들어오는지 확인한다.
4. 모바일 폭에서도 같은 6개 섹션을 다시 확인하고, `Poc`의 mobile steps grid가 깨지지 않는지 본다.
5. 이상 없으면 `approved`, 이상 있으면 섹션명과 증상을 적어 회신한다.
  </how-to-verify>
  <resume-signal>`approved` 또는 섹션별 이슈 목록으로 응답</resume-signal>
  <done>사용자 확인 결과가 확보되어 Phase 2 reveal 품질 상태를 종료 판단할 수 있다.</done>
</task>

</tasks>

<verification>
자동 검증은 `npm run lint && npm run build` 통과가 기준이고, 수동 검증은 데스크톱/모바일에서 6개 섹션 reveal과 `Poc` 3블록 독립 진입을 확인하는 것이다.
</verification>

<success_criteria>
Wave 1 적용분이 lint/build를 통과하고, 사용자가 6개 대상 섹션 reveal 결과를 승인하거나 정확한 수정 피드백을 남긴다.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-section-reveal/02-04-SUMMARY.md`
</output>
