---
phase: 02-static-section-reveal
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/BeyondScreenSection.tsx
  - src/components/sections/Web3Section.tsx
  - src/components/sections/PioneerSection.tsx
autonomous: true
requirements:
  - SECT-01
must_haves:
  truths:
    - "사용자는 BeyondScreen 섹션 제목과 문단이 같은 fadeUp 계열 reveal로 들어오는 것을 본다."
    - "사용자는 Web3 섹션 제목과 각 문단이 같은 리듬으로 순차 reveal 되는 것을 본다."
    - "사용자는 Pioneer 섹션에서 링 아트워크, 로고, 타이틀, 가격 CTA가 문서와 같은 순서로 reveal 되는 것을 본다."
  artifacts:
    - path: src/components/sections/BeyondScreenSection.tsx
      provides: "BeyondScreen 제목과 문단 reveal 배치"
    - path: src/components/sections/Web3Section.tsx
      provides: "Web3 제목과 문단 reveal 배치"
    - path: src/components/sections/PioneerSection.tsx
      provides: "Pioneer 미디어/텍스트/CTA reveal 배치"
  key_links:
    - from: src/components/sections/BeyondScreenSection.tsx
      to: src/components/shared/RevealOnScroll.tsx
      via: "제목과 문단 wrapper"
      pattern: "RevealOnScroll"
    - from: src/components/sections/Web3Section.tsx
      to: src/lib/motion.ts
      via: "fadeUp preset import"
      pattern: "fadeUp"
    - from: src/components/sections/PioneerSection.tsx
      to: src/lib/motion.ts
      via: "scaleUp/fadeIn/fadeUp preset import"
      pattern: "scaleUp|fadeIn|fadeUp"
---

<objective>
정적 텍스트/CTA 중심 섹션인 `BeyondScreen`, `Web3`, `Pioneer`에 공통 reveal 래퍼를 적용한다.

Purpose: SECT-01 범위의 저위험 섹션에서 동일한 reveal 톤을 먼저 고정해 Phase 2의 시각적 일관성을 만든다.
Output: 세 섹션이 서버 컴포넌트 상태를 유지한 채 `RevealOnScroll`과 공통 프리셋을 소비한다.
Prerequisite: Phase 1의 `01-01-PLAN.md`가 완료되어 `RevealOnScroll`과 `src/lib/motion.ts` 프리셋이 이미 존재하는 브랜치에서 실행한다.
</objective>

<execution_context>
@/Users/arisnoba/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arisnoba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-motion-foundation/01-01-PLAN.md
@.planning/phases/01-motion-foundation/01-CONTEXT.md
@docs/motion-plan.md
@src/components/sections/BeyondScreenSection.tsx
@src/components/sections/Web3Section.tsx
@src/components/sections/PioneerSection.tsx
@src/components/shared/SectionContainer.tsx
@src/components/shared/RingArtwork.tsx

Phase prerequisite: 이 plan은 `01-01-PLAN.md`가 먼저 적용되어 `src/components/shared/RevealOnScroll.tsx`와 `src/lib/motion.ts`가 이미 생성된 상태를 전제로 한다.

<interfaces>
`src/components/shared/SectionContainer.tsx`
```tsx
interface SectionContainerProps {
  children: React.ReactNode;
  className?: string;
}
```

Phase 1 contract from `docs/motion-plan.md` / `01-CONTEXT.md`
```tsx
interface RevealOnScrollProps {
  children: ReactNode;
  className?: string;
  variants?: Variants;
  delay?: number;
  duration?: number;
  once?: boolean;
  amount?: number;
  as?: 'div' | 'section' | 'article' | 'span';
}
```

Expected presets from `src/lib/motion.ts`
```ts
export const fadeUp: Variants
export const fadeIn: Variants
export const scaleUp: Variants
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: BeyondScreen과 Web3를 문단 중심 reveal로 전환</name>
  <files>src/components/sections/BeyondScreenSection.tsx, src/components/sections/Web3Section.tsx</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-motion-foundation/01-01-PLAN.md
.planning/phases/01-motion-foundation/01-CONTEXT.md
docs/motion-plan.md
src/components/sections/BeyondScreenSection.tsx
src/components/sections/Web3Section.tsx
  </read_first>
  <action>Phase 1 결정 D-01, D-03, D-05를 그대로 따른다. `BeyondScreenSection.tsx`와 `Web3Section.tsx`에만 `RevealOnScroll`과 `fadeUp` import를 추가하고 파일 상단에 `'use client'`를 넣지 않는다. `BeyondScreenSection.tsx`에서는 `<h2>`를 `RevealOnScroll variants={fadeUp}`로 감싸고, `paragraphs.map(...)`가 만드는 각 `<p>`를 `RevealOnScroll variants={fadeUp} delay={0.1 * index}`로 감싼다. `Web3Section.tsx`에서는 제목 전체 블록을 `RevealOnScroll variants={fadeUp}`로 감싸고, `paragraphs.map(...)`의 각 `<p>`를 `RevealOnScroll variants={fadeUp} delay={0.12 * index}`로 감싼다. 기존 `SectionBackground`, `SectionContainer`, section root className, `data-header-theme`는 그대로 유지하고 `HeroSection`, `TwoModesSection`, `ModeCardCarousel`, `Header`는 건드리지 않는다.</action>
  <verify>
    <automated>rg -n "RevealOnScroll|fadeUp|delay=\\{0\\.(1 \\* index|12 \\* index)" src/components/sections/BeyondScreenSection.tsx src/components/sections/Web3Section.tsx</automated>
  </verify>
  <acceptance_criteria>
- `src/components/sections/BeyondScreenSection.tsx`에 `import RevealOnScroll`와 `import { fadeUp } from '@/lib/motion'`가 존재한다.
- `BeyondScreenSection.tsx`의 `<h2>`가 `RevealOnScroll variants={fadeUp}` 내부로 이동한다.
- `BeyondScreenSection.tsx`의 문단 map 결과에 `delay={0.1 * index}`가 들어간 `RevealOnScroll`이 존재한다.
- `src/components/sections/Web3Section.tsx`에 `RevealOnScroll`와 `fadeUp` import가 존재한다.
- `Web3Section.tsx`의 문단 map 결과에 `delay={0.12 * index}`가 들어간 `RevealOnScroll`이 존재한다.
- 두 파일 어디에도 `'use client'` 문자열이 추가되지 않는다.
  </acceptance_criteria>
  <done>BeyondScreen과 Web3가 서버 컴포넌트를 유지하면서 제목/문단 reveal 배치를 갖고, 문단 delay 값이 계획 문서와 일치한다.</done>
</task>

<task type="auto">
  <name>Task 2: Pioneer 미디어와 가격 CTA reveal 순서를 고정</name>
  <files>src/components/sections/PioneerSection.tsx</files>
  <read_first>
.planning/ROADMAP.md
.planning/REQUIREMENTS.md
.planning/phases/01-motion-foundation/01-01-PLAN.md
.planning/phases/01-motion-foundation/01-CONTEXT.md
docs/motion-plan.md
src/components/sections/PioneerSection.tsx
src/components/shared/RingArtwork.tsx
src/lib/site.ts
  </read_first>
  <action>`PioneerSection.tsx`에 `RevealOnScroll`, `scaleUp`, `fadeIn`, `fadeUp` import를 추가한다. 왼쪽 미디어 컬럼에서는 `RingArtwork` wrapper를 `RevealOnScroll variants={scaleUp}`로 감싸고, 로고 `<img>`는 `RevealOnScroll variants={fadeIn} delay={0.15}`로 분리한다. 오른쪽 텍스트 컬럼에서는 제목을 `delay={0.1}`의 `fadeUp`, 본문 문단 묶음을 `delay={0.2}`의 `fadeUp`, 가격/버튼 묶음을 분리해 가격 텍스트는 `delay={0.25}`의 `fadeUp`, CTA 앵커는 `delay={0.3}`의 `fadeUp`으로 감싼다. 기존 레이아웃 그리드, waitlist 링크, hover className은 유지하고 section root에 `'use client'`를 추가하지 않는다.</action>
  <verify>
    <automated>rg -n "RevealOnScroll|scaleUp|fadeIn|fadeUp|0\\.15|0\\.25|0\\.3" src/components/sections/PioneerSection.tsx</automated>
  </verify>
  <acceptance_criteria>
- `src/components/sections/PioneerSection.tsx`에 `RevealOnScroll`, `scaleUp`, `fadeIn`, `fadeUp` import가 모두 존재한다.
- `RingArtwork`가 `RevealOnScroll variants={scaleUp}` 내부에 있다.
- 로고 `<img>`가 `RevealOnScroll variants={fadeIn} delay={0.15}` 내부에 있다.
- 가격 텍스트와 CTA 앵커가 각각 별도 `RevealOnScroll` wrapper로 분리되고 delay 값 `0.25`, `0.3`을 사용한다.
- `src/components/sections/PioneerSection.tsx`에 `'use client'`가 추가되지 않는다.
  </acceptance_criteria>
  <done>Pioneer 섹션이 링 아트워크 → 로고 → 타이틀/카피 → 가격/CTA 순서의 reveal 구조를 갖고 기존 CTA 링크 동작을 그대로 유지한다.</done>
</task>

</tasks>

<verification>
섹션 적용 후 `rg -n "RevealOnScroll|fadeUp|fadeIn|scaleUp" src/components/sections/BeyondScreenSection.tsx src/components/sections/Web3Section.tsx src/components/sections/PioneerSection.tsx`로 wrapper/preset 배치를 확인한다.
</verification>

<success_criteria>
`BeyondScreen`, `Web3`, `Pioneer` 세 파일이 모두 `RevealOnScroll`을 import하고, 계획 문서와 동일한 delay/preset 조합을 사용하며, 서버 컴포넌트 상태를 유지한다.
</success_criteria>

<output>
After completion, create `.planning/phases/02-static-section-reveal/02-01-SUMMARY.md`
</output>
