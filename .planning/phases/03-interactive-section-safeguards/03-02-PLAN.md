---
phase: 03-interactive-section-safeguards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/TwoModesSection.tsx
autonomous: true
requirements:
  - SECT-04
must_haves:
  truths:
    - "사용자는 TwoModes 섹션에서 제목, 제품 원형 배경+링 이미지, 버튼 그룹이 초기 진입 시 순서대로 reveal 된다."
    - "사용자는 reveal 이후에도 O/X 버튼과 중앙 전환 버튼으로 모드를 계속 전환할 수 있다."
    - "모드 전환 시 배경색, 링 슬라이드, 버튼 강조 상태가 기존처럼 동기화된다."
  artifacts:
    - path: "src/components/sections/TwoModesSection.tsx"
      provides: "initial reveal wrappers that preserve mode toggle state flow"
  key_links:
    - from: "src/components/sections/TwoModesSection.tsx"
      to: "src/lib/motion.ts"
      via: "headline/product/button group reveal presets"
      pattern: "RevealOnScroll"
    - from: "src/components/sections/TwoModesSection.tsx"
      to: "useState<'O' | 'X'>"
      via: "existing button handlers continue to drive state"
      pattern: "setMode\\("
---

<objective>
TwoModesSection에 초기 진입 reveal을 추가하되 기존 O/X 토글 인터랙션과 클라이언트 상태 흐름을 보존한다.

Purpose: SECT-04를 충족하면서 Phase 3의 고위험 상태 전환 섹션을 별도 plan으로 격리한다.
Output: `TwoModesSection.tsx`가 제품 클러스터와 버튼 그룹 reveal을 가지면서 기존 `mode` state 기반 전환 로직은 유지된 상태.
</objective>

<execution_context>
@/Users/arisnoba/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arisnoba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/motion-plan.md
@src/components/sections/TwoModesSection.tsx
@src/components/shared/RevealOnScroll.tsx
@src/lib/motion.ts

<interfaces>
From `src/components/sections/TwoModesSection.tsx`:

```tsx
const [mode, setMode] = useState<'O' | 'X'>('O');
```

기존 인터랙션 계약:
- 좌측 버튼: `onClick={() => setMode('O')}`
- 중앙 버튼: `onClick={() => setMode(mode === 'O' ? 'X' : 'O')}`
- 우측 버튼: `onClick={() => setMode('X')}`
- 좌우 버튼은 `aria-pressed`로 활성 상태를 노출한다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 제목과 제품 클러스터에 초기 reveal을 추가한다</name>
  <read_first>
    - docs/motion-plan.md
    - src/components/sections/TwoModesSection.tsx
    - src/components/shared/RevealOnScroll.tsx
    - src/lib/motion.ts
  </read_first>
  <files>src/components/sections/TwoModesSection.tsx</files>
  <action>`RevealOnScroll`, `fadeUp`, `scaleUp`를 import한다. 상단 `max-w-[860px] text-center` 제목 블록을 `RevealOnScroll variants={fadeUp}`로 감싸고, 제품 영역에서는 현재 `<div className="relative mt-14 flex w-full items-center justify-center md:mt-16">` 전체를 `RevealOnScroll variants={scaleUp} delay={0.15}`로 감싼다. 이 wrapper 안의 원형 배경 `<div ... style={{ backgroundColor: ... }} />`와 절대 배치된 링 이미지 슬라이더 DOM 관계는 그대로 유지한다. `mode` state, `transition-colors`, `transition-transform`, 인라인 `transform` 스타일은 수정하지 않는다.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/sections/TwoModesSection.tsx`가 `RevealOnScroll`, `fadeUp`, `scaleUp`를 import한다.
    - 제품 reveal wrapper가 원형 배경 div와 절대 배치 링 이미지 컨테이너를 함께 감싼다.
    - `const [mode, setMode] = useState<'O' | 'X'>('O')` 선언과 기존 `transform: translateX(...)` 로직이 그대로 남아 있다.
  </acceptance_criteria>
  <done>TwoModes 섹션의 제목과 제품 클러스터가 초기 진입 reveal을 갖되 기존 상태 기반 전환 구조는 변하지 않는다.</done>
</task>

<task type="auto">
  <name>Task 2: 버튼 그룹 reveal을 추가하면서 O/X 토글 시맨틱을 보존한다</name>
  <read_first>
    - docs/motion-plan.md
    - src/components/sections/TwoModesSection.tsx
    - src/components/shared/RevealOnScroll.tsx
    - src/lib/motion.ts
  </read_first>
  <files>src/components/sections/TwoModesSection.tsx</files>
  <action>현재 `<div className="mt-12 grid w-full max-w-[320px] grid-cols-[1fr_auto_1fr] ...">` 버튼 그룹 전체를 `RevealOnScroll variants={fadeUp} delay={0.3}`로 감싼다. 내부의 좌/중앙/우 버튼 순서, `onClick` 핸들러, `aria-pressed`, 아이콘 `filter` style, 텍스트 색상 조건부 클래스는 그대로 유지한다. 새로운 `useEffect`, `useRef`, 추가 state는 넣지 않는다. `section`과 `SectionContainer` 레벨의 기존 `overflow-hidden`, 정렬, 간격 클래스는 변경하지 않는다.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - 버튼 그룹 grid div가 `RevealOnScroll variants={fadeUp} delay={0.3}` wrapper 안에 있다.
    - `onClick={() => setMode('O')}`, `onClick={() => setMode(mode === 'O' ? 'X' : 'O')}`, `onClick={() => setMode('X')}` 세 핸들러가 그대로 존재한다.
    - 좌우 버튼의 `aria-pressed={mode === 'O'}` / `aria-pressed={mode === 'X'}`가 그대로 존재한다.
    - 파일에 새로운 `useEffect`, `useRef`, 추가 `useState` 선언이 생기지 않는다.
  </acceptance_criteria>
  <done>TwoModes 버튼 그룹이 초기 reveal을 가지면서 기존 토글 시맨틱과 상태 전환 로직을 그대로 유지한다.</done>
</task>

</tasks>

<verification>
전체 검증은 아래 순서로 실행한다.

1. `npm run lint`
2. `npm run build`

추가 확인 포인트:
- `TwoModesSection.tsx`의 `mode` state와 세 개의 버튼 핸들러가 그대로 유지되어야 한다.
- reveal 추가로 인해 `TwoModesSection.tsx`에 새 브라우저 effect 로직이 생기지 않아야 한다.
</verification>

<success_criteria>
- 제목, 제품 클러스터, 버튼 그룹이 각각 reveal wrapper를 가진다.
- O/X 토글에 필요한 세 버튼 핸들러와 `aria-pressed` 시그니처가 그대로 남는다.
- `TwoModesSection.tsx` 변경 후 lint/build가 통과한다.
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-section-safeguards/03-interactive-section-safeguards-02-SUMMARY.md`
</output>
