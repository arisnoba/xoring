---
phase: 03-interactive-section-safeguards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/SocialModeSection.tsx
  - src/components/sections/PrivateModeSection.tsx
autonomous: true
requirements:
  - SECT-02
must_haves:
  truths:
    - "사용자는 SocialMode 섹션에서 sticky 링/텍스트/캐러셀이 순서대로 reveal 되는 흐름을 본다."
    - "사용자는 PrivateMode 섹션에서 sticky 링/텍스트/캐러셀이 순서대로 reveal 되는 흐름을 본다."
    - "사용자는 reveal 이후에도 두 섹션에서 ModeCardCarousel을 드래그하거나 스와이프할 수 있다."
  artifacts:
    - path: "src/components/sections/SocialModeSection.tsx"
      provides: "SocialMode sticky-safe reveal wrapper composition"
    - path: "src/components/sections/PrivateModeSection.tsx"
      provides: "PrivateMode sticky-safe reveal wrapper composition"
  key_links:
    - from: "src/components/sections/SocialModeSection.tsx"
      to: "src/components/shared/ModeCardCarousel.tsx"
      via: "ModeCardCarousel 바깥 mt-20 wrapper reveal"
      pattern: "RevealOnScroll[\\s\\S]*<ModeCardCarousel"
    - from: "src/components/sections/PrivateModeSection.tsx"
      to: "src/components/shared/ModeCardCarousel.tsx"
      via: "ModeCardCarousel 바깥 mt-20 wrapper reveal"
      pattern: "RevealOnScroll[\\s\\S]*<ModeCardCarousel"
    - from: "src/components/sections/SocialModeSection.tsx"
      to: "src/lib/motion.ts"
      via: "mode-ring wrapper uses fadeIn only"
      pattern: "variants=\\{fadeIn\\}"
---

<objective>
SocialModeSection과 PrivateModeSection에 sticky/carousel 안전 규칙을 지키는 reveal wrapper를 추가한다.

Purpose: SECT-02를 충족하면서 sticky 레이아웃과 Swiper 동작을 그대로 유지한다.
Output: 두 섹션 파일이 `RevealOnScroll`과 공통 preset을 사용하되, `ModeCardCarousel` 내부 구현은 건드리지 않는 상태.
</objective>

<execution_context>
@/Users/arisnoba/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arisnoba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@docs/motion-plan.md
@docs/mode-card-carousel.md
@src/components/sections/SocialModeSection.tsx
@src/components/sections/PrivateModeSection.tsx
@src/components/shared/ModeCardCarousel.tsx
@src/components/shared/RevealOnScroll.tsx
@src/lib/motion.ts

<interfaces>
From `src/components/shared/ModeCardCarousel.tsx`:

```ts
interface ModeCardCarouselProps {
	cards: CardData[];
	images: string[];
	variant: 'light' | 'dark';
}
```

핵심 구조:
- `ModeCardCarousel`는 이미 `'use client'`이며 resize 기반 width extension 로직을 가진다.
- Phase 3에서는 캐러셀 자신이 아니라 `ModeCardCarousel`을 감싸는 부모 wrapper에만 reveal을 추가해야 한다.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SocialModeSection에 sticky-safe reveal wrapper를 추가한다</name>
  <read_first>
    - docs/motion-plan.md
    - docs/mode-card-carousel.md
    - src/components/sections/SocialModeSection.tsx
    - src/components/shared/ModeCardCarousel.tsx
    - src/components/shared/RevealOnScroll.tsx
    - src/lib/motion.ts
  </read_first>
  <files>src/components/sections/SocialModeSection.tsx</files>
  <action>`RevealOnScroll`, `fadeIn`, `fadeUp`를 import하고 서버 컴포넌트 상태를 유지한다. `lg:sticky lg:top-28` 컬럼 안에서는 `<SectionBadge ... />`를 `RevealOnScroll variants={fadeUp}`로 감싸고, 바로 아래 `<div className="mt-8 mode-ring">...</div>`는 `RevealOnScroll variants={fadeIn} delay={0.1}`로 별도 감싼다. `mode-ring`에는 `scaleUp`이나 `y` transform preset을 쓰지 않는다. 우측 컬럼에서는 현재 headline 블록과 `<p className="section-copy...">`를 각각 `RevealOnScroll variants={fadeUp}`로 분리하고 delay를 `0.15`, `0.2`로 준다. 기존 `<div className="mt-20">`는 유지한 채 그 바깥을 `RevealOnScroll variants={fadeUp} delay={0.3}`로 감싸 `ModeCardCarousel` JSX 자체는 수정하지 않는다. `SectionBackground`, `SectionContainer`, grid/sticky class, `socialSlides`, `variant="light"`는 그대로 둔다.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/sections/SocialModeSection.tsx`가 `RevealOnScroll`, `fadeIn`, `fadeUp`를 import한다.
    - `.mode-ring` 블록이 `variants={fadeIn}` wrapper 안에 있고, 파일에 `scaleUp` 문자열이 추가되지 않는다.
    - `<ModeCardCarousel cards={SOCIAL_MODE.cards} images={socialSlides} variant="light" />`가 그대로 남고 바깥 wrapper에서만 reveal 된다.
    - 파일 최상단에 `'use client'`가 추가되지 않는다.
  </acceptance_criteria>
  <done>SocialModeSection이 sticky-safe reveal 구조로 재배치되고 carousel 내부 구현은 untouched 상태다.</done>
</task>

<task type="auto">
  <name>Task 2: PrivateModeSection에 동일한 sticky/carousel 보호 reveal을 적용한다</name>
  <read_first>
    - docs/motion-plan.md
    - docs/mode-card-carousel.md
    - src/components/sections/PrivateModeSection.tsx
    - src/components/shared/ModeCardCarousel.tsx
    - src/components/shared/RevealOnScroll.tsx
    - src/lib/motion.ts
  </read_first>
  <files>src/components/sections/PrivateModeSection.tsx</files>
  <action>`RevealOnScroll`, `fadeIn`, `fadeUp`를 import하고 파일을 server component로 유지한다. `lg:sticky lg:top-28` 컬럼에서 `SectionBadge`는 `fadeUp`, `.mode-ring`은 `fadeIn` + `delay={0.1}`로 분리 래핑한다. 우측 콘텐츠 컬럼에서는 headline 블록을 `fadeUp delay={0.15}`, subtext `<p>`를 `fadeUp delay={0.2}`, `<div className="mt-20">` 캐러셀 블록을 `fadeUp delay={0.3}`로 감싼다. `ModeCardCarousel`는 그대로 유지하고 `variant="dark"` 및 기존 grid/sticky/layout 클래스는 변경하지 않는다. `mode-ring`에는 `scaleUp`, translate, custom transform style을 추가하지 않는다.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/sections/PrivateModeSection.tsx`가 `RevealOnScroll`, `fadeIn`, `fadeUp`를 import한다.
    - `.mode-ring` 블록이 `variants={fadeIn}` wrapper 안에 있고 sticky column 구조(`lg:sticky lg:top-28`)가 그대로 남는다.
    - `<ModeCardCarousel cards={PRIVATE_MODE.cards} images={privateSlides} variant="dark" />`가 그대로 남고 바깥 wrapper에서만 reveal 된다.
    - 파일 최상단에 `'use client'`가 추가되지 않는다.
  </acceptance_criteria>
  <done>PrivateModeSection이 SocialMode와 같은 보호 패턴으로 reveal을 갖고, sticky/carousel 구조는 기존과 동일하다.</done>
</task>

</tasks>

<verification>
전체 검증은 아래 순서로 실행한다.

1. `npm run lint`
2. `npm run build`

추가 확인 포인트:
- 두 섹션 파일 모두 `ModeCardCarousel` import 경로와 JSX 시그니처가 바뀌지 않아야 한다.
- `src/components/shared/ModeCardCarousel.tsx`는 이 plan에서 수정하지 않는다.
</verification>

<success_criteria>
- `SocialModeSection`과 `PrivateModeSection`에 badge/ring/text/carousel reveal wrapper가 각각 존재한다.
- `.mode-ring` reveal은 `fadeIn`만 사용하며 sticky 관련 클래스가 유지된다.
- `ModeCardCarousel`는 두 섹션 모두에서 바깥 wrapper reveal만 추가된 상태로 남아 lint/build를 통과한다.
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-section-safeguards/03-interactive-section-safeguards-01-SUMMARY.md`
</output>
