---
phase: 04-hero-intro-release-readiness
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - .planning/phases/04-hero-intro-release-readiness/04-RELEASE-CHECKLIST.md
autonomous: false
requirements:
  - QUAL-02
  - QUAL-03
user_setup: []
must_haves:
  truths:
    - 프로젝트는 Hero intro 반영 이후에도 `npm run lint`와 `npm run build`를 통과한다.
    - 개발자는 데스크톱과 모바일에서 Hero 포함 전체 reveal 구간을 같은 체크리스트로 검증할 수 있다.
    - 수동 QA 결과를 보고 릴리스 가능 여부를 명확히 판단할 수 있다.
    - reduced motion 환경 검증이 체크리스트에 포함되고 실패 시 릴리스 결정은 `blocked`가 된다.
  artifacts:
    - path: .planning/phases/04-hero-intro-release-readiness/04-RELEASE-CHECKLIST.md
      provides: automated gate 기록, desktop/mobile QA 절차, pass/fail sign-off
      contains:
        - Automated Verification
        - Desktop QA
        - Mobile QA
        - Release Decision
  key_links:
    - from: .planning/phases/04-hero-intro-release-readiness/04-RELEASE-CHECKLIST.md
      to: src/app/page.tsx
      via: section order mirrored in checklist
      pattern: HeroSection|BeyondScreenSection|TwoModesSection|SocialModeSection|PrivateModeSection|AIAgentSection|ApplicationSection|PocSection|Web3Section|PioneerSection
    - from: execution commands
      to: repository quality gate
      via: npm run lint and npm run build
      pattern: npm run lint|npm run build
---

<objective>
릴리스 readiness를 자동 검증과 수동 QA 체크리스트로 고정한다.

Purpose: 테스트 프레임워크가 없는 현재 저장소에서 릴리스 판단 기준을 문서와 실행 결과로 남긴다.
Output: Phase 디렉터리에 자동 검증 기록과 데스크톱, 모바일 QA 절차가 포함된 체크리스트가 생성된다.
</objective>

<execution_context>
@/Users/arisnoba/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arisnoba/.codex/get-shit-done/templates/summary.md
@/Users/arisnoba/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/motion-plan.md
@src/app/page.tsx
@src/components/sections/HeroSection.tsx

<interfaces>
From `src/app/page.tsx` section order:
```tsx
<HeroSection />
<BeyondScreenSection />
<TwoModesSection />
<SocialModeSection />
<PrivateModeSection />
<AIAgentSection />
<ApplicationSection />
<PocSection />
<Web3Section />
<PioneerSection />
```

From `CLAUDE.md`:
```bash
npm run dev
npm run build
npm run lint
```

Release checklist must validate:
- Hero intro first impression
- Hero scrub, overlay, header theme coexistence
- Phase 2, 3 reveal sections in page order
- Desktop, mobile viewport coverage
- reduced motion에서 intro와 reveal이 즉시 노출되는지 여부
</interfaces>

가정:
- 이 plan 시점에는 Phase 1~4 구현이 모두 작업 브랜치에 반영되어 있다.
- 수동 QA는 체크리스트 기반 human gate로 처리하며, 새 테스트 프레임워크는 도입하지 않는다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: 릴리스 체크리스트를 Phase 디렉터리에 만든다</name>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - docs/motion-plan.md
    - src/app/page.tsx
    - src/components/sections/HeroSection.tsx
  </read_first>
  <files>.planning/phases/04-hero-intro-release-readiness/04-RELEASE-CHECKLIST.md</files>
  <action>
    `.planning/phases/04-hero-intro-release-readiness/04-RELEASE-CHECKLIST.md`를 새로 만들고 다음 섹션을 고정한다: `## Automated Verification`, `## Desktop QA`, `## Mobile QA`, `## Reduced Motion QA`, `## Release Decision`. `src/app/page.tsx`의 실제 렌더 순서대로 Hero, BeyondScreen, TwoModes, SocialMode, PrivateMode, AIAgent, Application, Poc, Web3, Pioneer를 모두 체크 항목에 포함한다. Hero 항목에는 `logo/ring/store-buttons intro`, `overlay scrub`, `header theme sync`를 분리된 검증 포인트로 적고, interactive 구간에는 `TwoModes` 토글, `ModeCardCarousel` 스와이프 유지 항목을 넣는다. `Reduced Motion QA` 섹션에는 Hero intro 미재생, 주요 reveal 섹션 즉시 노출, 콘텐츠 가독성 유지 여부를 기록하게 한다. 각 항목은 `Expected`, `Viewport`, `Status`, `Notes`를 기록할 수 있는 표 또는 체크리스트 형식으로 작성한다.
  </action>
  <acceptance_criteria>
    - `04-RELEASE-CHECKLIST.md`에 `## Automated Verification`, `## Desktop QA`, `## Mobile QA`, `## Reduced Motion QA`, `## Release Decision` 헤더가 모두 존재한다.
    - 체크리스트에 `HeroSection`, `BeyondScreenSection`, `TwoModesSection`, `SocialModeSection`, `PrivateModeSection`, `AIAgentSection`, `ApplicationSection`, `PocSection`, `Web3Section`, `PioneerSection` 이름이 모두 포함된다.
    - `rg -n "^## Automated Verification|^## Desktop QA|^## Mobile QA|^## Reduced Motion QA|^## Release Decision|HeroSection|PioneerSection" .planning/phases/04-hero-intro-release-readiness/04-RELEASE-CHECKLIST.md` 가 필요한 항목을 검출한다.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>릴리스 판단에 필요한 자동 검증과 수동 QA 절차가 한 파일에 정리된다.</done>
</task>

<task type="auto">
  <name>Task 2: lint와 build를 실행하고 체크리스트에 자동 검증 결과를 기록한다</name>
  <read_first>
    - .planning/phases/04-hero-intro-release-readiness/04-RELEASE-CHECKLIST.md
    - CLAUDE.md
  </read_first>
  <files>.planning/phases/04-hero-intro-release-readiness/04-RELEASE-CHECKLIST.md</files>
  <action>
    저장소 루트에서 `npm run lint`와 `npm run build`를 실행한다. 결과가 모두 성공하면 `04-RELEASE-CHECKLIST.md`의 `Automated Verification` 섹션에 각 명령의 실행 여부와 성공 상태를 `PASS`로 기록한다. 실패하면 실패한 명령, 에러 요약, 재현 명령을 같은 섹션에 남기고 `Release Decision`은 자동으로 `blocked` 상태로 둔다. reduced motion 검증이 비어 있거나 실패한 경우도 `Release Decision`을 `blocked`로 유지하도록 체크리스트 구조를 작성한다. 로그 원문을 길게 복사하지 말고 실패 지점만 짧게 요약한다.
  </action>
  <acceptance_criteria>
    - `04-RELEASE-CHECKLIST.md`의 `Automated Verification` 섹션에 `npm run lint`와 `npm run build` 두 명령이 모두 기록된다.
    - 두 명령의 상태가 `PASS` 또는 `FAIL`로 명시된다.
    - `rg -n "npm run lint|npm run build|PASS|FAIL|blocked|Reduced Motion QA" .planning/phases/04-hero-intro-release-readiness/04-RELEASE-CHECKLIST.md` 로 결과를 확인할 수 있다.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>자동 품질 게이트의 성공, 실패 여부가 체크리스트에 남고 릴리스 차단 조건이 문서화된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 데스크톱과 모바일 수동 QA로 릴리스 승인 여부를 확정한다</name>
  <read_first>
    - .planning/phases/04-hero-intro-release-readiness/04-RELEASE-CHECKLIST.md
    - docs/motion-plan.md
    - src/app/page.tsx
  </read_first>
  <action>
    `npm run dev`로 로컬 서버를 띄운 뒤 체크리스트 순서대로 데스크톱, 모바일, reduced motion 뷰포트에서 전체 랜딩을 검증한다. Hero는 첫 진입 intro, overlay scrub, header theme sync를 각각 확인하고, 나머지 섹션은 reveal 시점과 interactive 회귀 여부를 체크한다. `TwoModes` 토글, `SocialMode`와 `PrivateMode` 캐러셀, `Poc`의 블록 단위 reveal, 최하단 `Pioneer` CTA까지 모두 확인한다. reduced motion 시나리오에서는 Hero intro가 재생되지 않고 주요 reveal 섹션 콘텐츠가 즉시 노출되는지 별도로 기록한다. 결과는 체크리스트 파일에 `PASS` 또는 `FAIL`과 재현 메모로 남기고, 하나라도 실패하면 최종 `Release Decision`을 `blocked`로 채운다.
  </action>
  <what-built>자동 검증이 기록된 릴리스 체크리스트와 Hero intro가 반영된 전체 랜딩</what-built>
  <how-to-verify>
    1. `npm run dev` 실행 후 데스크톱 뷰포트에서 페이지를 새로고침한다.
    2. Hero 로고, 링, 다운로드 버튼의 intro 순서와 자연스러운 종료를 확인한다.
    3. Hero를 스크롤하며 overlay 전환과 header theme sync가 유지되는지 확인한다.
    4. 페이지 하단까지 스크롤하며 각 섹션 reveal을 체크리스트에 따라 확인한다.
    5. 모바일 뷰포트로 전환해 같은 순서를 반복한다.
    6. 브라우저 개발자 도구 또는 OS 설정으로 `prefers-reduced-motion: reduce`를 켜고 Hero intro 미재생과 주요 섹션 즉시 노출을 확인한다.
    7. 체크리스트의 `Release Decision`을 `approved` 또는 `blocked`로 기록한다.
  </how-to-verify>
  <resume-signal>`approved` 또는 실패 항목 목록을 포함한 메시지로 재개한다.</resume-signal>
  <acceptance_criteria>
    - 체크리스트 파일에 데스크톱과 모바일 결과가 각각 채워진다.
    - 체크리스트 파일에 reduced motion 결과가 채워진다.
    - `Release Decision`이 `approved` 또는 `blocked` 중 하나로 기록된다.
    - Hero, TwoModes, SocialMode, PrivateMode, Poc, Pioneer 관련 상태가 모두 비어 있지 않다.
  </acceptance_criteria>
  <verify>
    <automated>npm run dev</automated>
  </verify>
  <done>수동 QA 결과가 남고 릴리스 가능 여부가 명확하게 결정된다.</done>
</task>

</tasks>

<verification>
- `npm run lint`
- `npm run build`
- human checkpoint: `npm run dev` 기준 데스크톱, 모바일, reduced motion 수동 QA
</verification>

<success_criteria>
- 릴리스 체크리스트가 Phase 디렉터리에 존재한다.
- 자동 검증 결과가 기록되어 있다.
- 데스크톱, 모바일, reduced motion QA 결과와 최종 승인 여부가 문서화되어 있다.
</success_criteria>

<output>
After completion, create `.planning/phases/04-hero-intro-release-readiness/04-02-SUMMARY.md`
</output>
