---
phase: 04-hero-intro-release-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/HeroSection.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02
user_setup: []
must_haves:
  truths:
    - 사용자는 첫 진입 시 Hero 로고, 링, 다운로드 버튼이 순차적으로 등장하는 초기 모션을 본다.
    - Hero 초기 등장 모션은 기존 overlay scrub, 헤더 테마 전환, Lenis-ScrollTrigger 브리지와 충돌하지 않는다.
    - Hero 상단 정지 상태와 스크롤 진행 상태의 시각 전환이 자연스럽고 되감기나 깜빡임 없이 유지된다.
    - reduced motion 환경에서는 Hero intro가 재생되지 않고 로고, 링, 다운로드 버튼이 즉시 표시된다.
  artifacts:
    - path: src/components/sections/HeroSection.tsx
      provides: GSAP one-shot intro timeline, selector hooks, scrub coexistence guard
      contains:
        - introTl
        - hero-logo
        - hero-store-buttons
  key_links:
    - from: src/components/sections/HeroSection.tsx
      to: src/components/layout/Header.tsx
      via: document.documentElement.dataset.heroHeaderTheme + xoring:hero-header-theme-change
      pattern: heroHeaderTheme|xoring:hero-header-theme-change
    - from: src/components/sections/HeroSection.tsx
      to: src/components/providers/SmoothScrollProvider.tsx
      via: existing ScrollTrigger lifecycle preserved under gsap.context cleanup
      pattern: ScrollTrigger|ctx.revert
---

<objective>
Hero 첫 진입 모션을 기존 scrub 로직과 분리된 GSAP one-shot 타임라인으로 추가한다.

Purpose: 기존 히어로 스크롤 경험을 깨지 않으면서 첫인상 품질을 올린다.
Output: `src/components/sections/HeroSection.tsx`에 intro 전용 타임라인과 충돌 방지 구조가 반영된다.
</objective>

<execution_context>
@/Users/arisnoba/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arisnoba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@docs/motion-plan.md
@src/components/sections/HeroSection.tsx
@src/components/layout/Header.tsx
@src/components/shared/StoreButtons.tsx
@src/components/providers/SmoothScrollProvider.tsx

<interfaces>
From `src/components/shared/StoreButtons.tsx`:
```tsx
interface StoreButtonsProps {
	className?: string;
	variant?: 'dark' | 'light';
	googleFirst?: boolean;
	buttonClassName?: string;
}
```

From `src/components/sections/HeroSection.tsx`:
```tsx
const introLeftRef = useRef<HTMLDivElement>(null);
const introRingRef = useRef<HTMLDivElement>(null);

const syncHeroHeaderTheme = (nextTheme: 'light' | 'dark') => {
	if (heroHeaderTheme === nextTheme) return;
	heroHeaderTheme = nextTheme;
	document.documentElement.dataset.heroHeaderTheme = nextTheme;
	window.dispatchEvent(new Event('xoring:hero-header-theme-change'));
};
```

From `docs/motion-plan.md`:
- Hero intro는 `motion`이 아니라 GSAP one-shot 타임라인으로 처리한다.
- intro 대상은 로고, 링, 다운로드 버튼이다.
- 기존 overlay text scrub 타임라인은 변경하지 않는다.
- 같은 DOM 노드의 같은 속성을 intro와 scrub이 동시에 만지지 않도록 노드를 분리하거나 대상 범위를 명확히 유지한다.
- 접근성 제약상 reduced motion 사용자는 모션 없이 즉시 콘텐츠를 봐야 한다.
</interfaces>

가정:
- Phase 1~3의 reveal 관련 변경은 이미 merge 되었다고 가정하고, 이 plan은 Hero 전용 초기 진입 모션만 다룬다.
- 새 라이브러리 추가나 새로운 클라이언트 경계 확장은 하지 않는다.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero intro 대상 셀렉터와 one-shot 타임라인을 추가한다</name>
  <read_first>
    - src/components/sections/HeroSection.tsx
    - docs/motion-plan.md
    - src/components/shared/StoreButtons.tsx
  </read_first>
  <files>src/components/sections/HeroSection.tsx</files>
  <action>
    `HeroSection.tsx` 안에서 로고, 스토어 버튼, 링 래퍼를 intro 전용 대상으로 명확히 식별할 수 있게 className 또는 직접 ref 선택 경로를 추가한다. `docs/motion-plan.md`의 Hero 규칙대로 `gsap.context` 내부에서 ScrollTrigger scrub 타임라인 생성보다 먼저 `introTl` one-shot 타임라인을 만든다. 로고는 `y: 40, opacity: 0`, 링은 `y: 60, scale: 0.92, opacity: 0`, 스토어 버튼은 `y: 40, opacity: 0`에서 시작하고 delay는 각각 `0`, `0.15`, `0.3`으로 둔다. 동시에 `window.matchMedia('(prefers-reduced-motion: reduce)')` 또는 이미 도입된 공용 motion guard를 확인해 reduced motion일 때는 `introTl` 자체를 만들지 않고 로고, 링, 버튼을 즉시 visible 상태로 둔다. overlay, overlayText, scrub 진행률 기반 `syncHeroHeaderTheme` 로직은 삭제하거나 순서를 바꾸지 말고 유지한다. `StoreButtons` 컴포넌트 자체는 수정하지 말고 `HeroSection`에서 이미 제공되는 `className`이나 wrapper를 이용해 target hook을 건다.
  </action>
  <acceptance_criteria>
    - `src/components/sections/HeroSection.tsx`에 `introTl` 식별자와 `.hero-logo`, `.hero-store-buttons` 또는 동등한 intro target hook이 존재한다.
    - `HeroSection.tsx`에서 intro 타임라인 정의가 ScrollTrigger timeline 정의보다 앞에 있다.
    - `HeroSection.tsx`에 `prefers-reduced-motion` 분기 또는 동등한 reduced motion guard가 존재하고, 그 분기에서 intro를 건너뛴다는 코드가 드러난다.
    - `rg -n "introTl|hero-logo|hero-store-buttons|gsap\\.timeline\\(" src/components/sections/HeroSection.tsx` 실행 시 관련 코드가 검출된다.
    - `rg -n "reduced-motion|prefers-reduced-motion|matchMedia" src/components/sections/HeroSection.tsx` 실행 시 guard 관련 코드가 검출된다.
  </acceptance_criteria>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Hero 첫 화면의 핵심 3요소가 one-shot GSAP timeline에 연결되고, 기존 scrub timeline과 별개로 실행된다.</done>
</task>

<task type="auto">
  <name>Task 2: scrub 충돌과 브라운필드 회귀 위험을 차단한다</name>
  <read_first>
    - src/components/sections/HeroSection.tsx
    - src/components/layout/Header.tsx
    - src/components/providers/SmoothScrollProvider.tsx
    - docs/motion-plan.md
  </read_first>
  <files>src/components/sections/HeroSection.tsx</files>
  <action>
    Hero intro가 브라운필드 동작을 깨지 않도록 `HeroSection.tsx`를 정리한다. intro 타임라인은 intro 영역의 로고, 버튼, 링 wrapper에만 적용하고, overlay 및 overlayText의 scrub 속성(`opacity`, `y`)에는 손대지 않는다. `document.documentElement.dataset.heroHeaderTheme` 초기화, `xoring:hero-header-theme-change` dispatch, `ctx.revert()` cleanup은 그대로 유지한다. reduced motion 분기에서는 intro 대상 요소를 즉시 보여주고, 추가 애니메이션이나 별도 전역 상태를 만들지 않는다. 필요하면 짧은 주석 한 줄로 intro timeline과 scrub timeline의 역할 분리를 설명하되, 영구 로그나 디버그 코드는 남기지 않는다. reduced motion 처리가 Phase 1에서 별도 util에 반영돼 있더라도, Hero intro가 그 접근성 제약을 우회하지 않도록 제한한다.
  </action>
  <acceptance_criteria>
    - `HeroSection.tsx`에 `scrollTrigger`가 여전히 1개 흐름으로 유지되고 `onUpdate` 안의 `syncHeroHeaderTheme` 호출이 보존된다.
    - `ctx.revert()` cleanup과 `delete document.documentElement.dataset.heroHeaderTheme`가 제거되지 않는다.
    - reduced motion 분기에서 intro 대상 요소를 즉시 표시하거나 `introTl` 생성을 건너뛴다는 코드가 존재한다.
    - `rg -n "scrollTrigger:|syncHeroHeaderTheme|ctx\\.revert|delete document\\.documentElement\\.dataset\\.heroHeaderTheme" src/components/sections/HeroSection.tsx` 가 모두 매치된다.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Hero intro 추가 후에도 정적 export 빌드가 가능하고, 기존 헤더 테마 sync 및 overlay scrub 연결이 코드 수준에서 유지된다.</done>
</task>

</tasks>

<verification>
- `npm run lint`
- `npm run build`
- reduced motion 환경에서 Hero intro가 즉시 표시되는지 브라우저 개발자 도구의 emulate setting 또는 OS 설정으로 확인한다.
- 구현 후 diff에서 `HeroSection.tsx` 외의 불필요한 파일 수정이 없는지 확인한다.
</verification>

<success_criteria>
- Hero 초기 진입 모션이 로고, 링, 다운로드 버튼에만 적용된다.
- 기존 overlay scrub, header theme sync, Lenis-ScrollTrigger 브리지가 그대로 유지된다.
- reduced motion 환경에서는 Hero intro가 재생되지 않고 콘텐츠가 즉시 노출된다.
- 빌드와 린트가 모두 성공한다.
</success_criteria>

<output>
After completion, create `.planning/phases/04-hero-intro-release-readiness/04-01-SUMMARY.md`
</output>
