---
phase: 01-motion-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/motion.ts
  - src/components/shared/RevealOnScroll.tsx
  - scripts/verify-motion-foundation.mjs
  - package.json
autonomous: true
requirements:
  - MOTN-01
  - MOTN-02
  - MOTN-03
  - QUAL-01
must_haves:
  truths:
    - 개발자는 섹션 파일을 클라이언트로 승격하지 않고 `RevealOnScroll` 래퍼 하나로 reveal을 적용할 수 있다.
    - 개발자는 `src/lib/motion.ts`에서 동일한 reveal 리듬의 preset을 import할 수 있다.
    - reduced motion 환경에서는 reveal 대상이 스크롤 애니메이션 없이 즉시 표시된다.
    - Phase 1 완료 후에도 기존 서버 섹션들은 `'use client'` 없이 유지된다.
  artifacts:
    - path: src/lib/motion.ts
      provides: restrained reveal preset and shared timing constants
      exports:
        - revealEase
        - defaultRevealDuration
        - defaultRevealAmount
        - fadeUp
        - fadeIn
        - scaleUp
    - path: src/components/shared/RevealOnScroll.tsx
      provides: reusable client-only reveal wrapper with reduced-motion fallback
      exports:
        - default
    - path: scripts/verify-motion-foundation.mjs
      provides: architectural guardrail for client boundary regression
    - path: package.json
      provides: runnable motion foundation verification command
  key_links:
    - from: src/components/shared/RevealOnScroll.tsx
      to: src/lib/motion.ts
      via: imported preset and timing defaults
      pattern: "from '@/lib/motion'"
    - from: src/components/shared/RevealOnScroll.tsx
      to: motion/react
      via: useReducedMotion gated initial/whileInView behavior
      pattern: "useReducedMotion"
    - from: scripts/verify-motion-foundation.mjs
      to: src/components/sections/BeyondScreenSection.tsx
      via: server boundary assertion against accidental 'use client'
      pattern: "BeyondScreenSection.tsx"
---

<objective>
Phase 1의 foundation 범위를 공통 reveal primitive와 reduced motion fallback, 서버 컴포넌트 경계 guardrail까지 포함해 잠근다.

Purpose: 이후 phase가 섹션별 reveal을 추가할 때 reduced motion 처리와 client boundary 원칙을 다시 흔들지 않게 한다.
Output: 공통 motion preset 모듈, `RevealOnScroll` 클라이언트 래퍼, server/client 경계 검증 스크립트
</objective>

<execution_context>
@/Users/arisnoba/.codex/get-shit-done/workflows/execute-plan.md
@/Users/arisnoba/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-motion-foundation/01-CONTEXT.md
@docs/motion-plan.md
@.planning/codebase/CONVENTIONS.md
@src/components/shared/SectionBackground.tsx
@src/components/shared/SectionContainer.tsx
@src/components/sections/HeroSection.tsx
@src/app/page.tsx
@package.json

<interfaces>
From src/lib/utils.ts:
```ts
export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

From src/components/shared/SectionContainer.tsx:
```tsx
interface SectionContainerProps {
  children: React.ReactNode;
  className?: string;
}
```

From src/components/shared/SectionBackground.tsx:
```tsx
'use client';

useEffect(() => {
  const observer = new IntersectionObserver(...);
  return () => observer.disconnect();
}, [shouldLoad]);
```

From src/components/sections/HeroSection.tsx:
```tsx
'use client';
gsap.registerPlugin(ScrollTrigger);
```

Current section client boundaries from src/app/page.tsx and repo scan:
```text
Client sections: HeroSection.tsx, TwoModesSection.tsx
Server sections: BeyondScreenSection.tsx, SocialModeSection.tsx, PrivateModeSection.tsx,
  AIAgentSection.tsx, ApplicationSection.tsx, PocSection.tsx, Web3Section.tsx, PioneerSection.tsx
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 공통 reveal preset 계약을 `src/lib/motion.ts`에 정의한다</name>
  <files>src/lib/motion.ts</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-motion-foundation/01-CONTEXT.md
    - docs/motion-plan.md
    - src/lib/utils.ts
  </read_first>
  <action>
    `src/lib/motion.ts`를 새로 만들고 `motion/react`의 `Variants`를 사용해 named export만 제공한다. D-05/D-06에 맞춰 과장 없는 수치로 고정한다: `revealEase = [0.25, 0.1, 0.25, 1]`, `defaultRevealDuration = 0.7`, `defaultRevealAmount = 0.15`. Variant는 세 가지만 만든다. `fadeUp.hidden = { opacity: 0, y: 32 }`, `fadeUp.visible = { opacity: 1, y: 0 }`, `fadeIn.hidden = { opacity: 0 }`, `fadeIn.visible = { opacity: 1 }`, `scaleUp.hidden = { opacity: 0, y: 24, scale: 0.96 }`, `scaleUp.visible = { opacity: 1, y: 0, scale: 1 }`. D-04에 따라 Phase 1 범위를 넘는 `stagger`, `children`, `timeline`, `slideFromLeft` 같은 preset은 추가하지 않는다.
  </action>
  <acceptance_criteria>
    - `rg -n "export const revealEase = \\[0\\.25, 0\\.1, 0\\.25, 1\\]" src/lib/motion.ts` 가 1건 이상 매치된다.
    - `rg -n "export const defaultRevealDuration = 0\\.7|export const defaultRevealAmount = 0\\.15" src/lib/motion.ts` 가 모두 매치된다.
    - `rg -n "export const fadeUp|export const fadeIn|export const scaleUp" src/lib/motion.ts` 가 세 preset 모두를 찾는다.
    - `src/lib/motion.ts`에 `slideFromLeft`, `stagger`, `timeline` 문자열이 없다.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "export const revealEase = \\[0\\.25, 0\\.1, 0\\.25, 1\\]|export const defaultRevealDuration = 0\\.7|export const defaultRevealAmount = 0\\.15|export const fadeUp|export const fadeIn|export const scaleUp" src/lib/motion.ts</automated>
  </verify>
  <done>`src/lib/motion.ts` 하나만 import해도 reveal timing과 preset 세트를 재사용할 수 있다.</done>
</task>

<task type="auto">
  <name>Task 2: `RevealOnScroll` 래퍼에서 reduced motion fallback을 중앙집중식으로 처리한다</name>
  <files>src/components/shared/RevealOnScroll.tsx</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-motion-foundation/01-CONTEXT.md
    - docs/motion-plan.md
    - src/components/shared/SectionBackground.tsx
    - src/components/shared/SectionContainer.tsx
    - src/lib/motion.ts
    - src/lib/utils.ts
  </read_first>
  <action>
    `src/components/shared/RevealOnScroll.tsx`를 `'use client';`로 생성한다. `motion/react`에서 `motion`, `useReducedMotion`, `type Variants`를 import하고 `cn`과 Task 1의 preset 상수를 사용한다. API는 D-03을 그대로 따른다: `children`, `className`, `variants`, `delay`, `duration`, `once`, `amount`, `as`. 태그는 `div | section | article | span`만 허용하는 `motionTags` 명시 맵으로 처리한다. 기본값은 `as='div'`, `variants=fadeUp`, `delay=0`, `duration=defaultRevealDuration`, `once=true`, `amount=defaultRevealAmount`로 고정한다. D-01/D-02를 지키기 위해 reduced motion일 때는 `initial="visible"`과 `whileInView={undefined}` 또는 동일한 즉시 노출 동작을 사용해 애니메이션을 완전히 우회한다. reduced motion이 아닐 때만 `initial="hidden"`, `whileInView="visible"`, `viewport={{ once, amount }}`, `transition={{ duration, delay, ease: revealEase }}`를 준다. D-04에 따라 Phase 1에서는 `staggerChildren`, `AnimatePresence`, children sequencing prop, GSAP interop prop을 추가하지 않는다.
  </action>
  <acceptance_criteria>
    - `rg -n "^'use client';" src/components/shared/RevealOnScroll.tsx` 가 매치된다.
    - `rg -n "useReducedMotion|const motionTags =|as = 'div'|variants = fadeUp" src/components/shared/RevealOnScroll.tsx` 가 모두 매치된다.
    - `rg -n "viewport=\\{\\{ once, amount \\}\\}|transition=\\{\\{ duration, delay, ease: revealEase \\}\\}" src/components/shared/RevealOnScroll.tsx` 가 매치된다.
    - `rg -n "staggerChildren|AnimatePresence|timeline|childrenVariants" src/components/shared/RevealOnScroll.tsx` 결과가 비어 있다.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^'use client';|useReducedMotion|const motionTags =|variants = fadeUp|viewport=\\{\\{ once, amount \\}\\}|transition=\\{\\{ duration, delay, ease: revealEase \\}\\}" src/components/shared/RevealOnScroll.tsx</automated>
  </verify>
  <done>`RevealOnScroll` 하나가 client boundary와 reduced motion fallback을 모두 담당하고, 서버 섹션은 이 컴포넌트를 자식으로 import하는 것만으로 foundation을 사용할 수 있다.</done>
</task>

<task type="auto">
  <name>Task 3: 서버 컴포넌트 경계 회귀를 막는 검증 스크립트와 npm 명령을 추가한다</name>
  <files>scripts/verify-motion-foundation.mjs, package.json</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/phases/01-motion-foundation/01-CONTEXT.md
    - docs/motion-plan.md
    - package.json
    - src/app/page.tsx
    - src/components/sections/BeyondScreenSection.tsx
    - src/components/sections/SocialModeSection.tsx
    - src/components/sections/PrivateModeSection.tsx
    - src/components/sections/AIAgentSection.tsx
    - src/components/sections/ApplicationSection.tsx
    - src/components/sections/PocSection.tsx
    - src/components/sections/Web3Section.tsx
    - src/components/sections/PioneerSection.tsx
    - src/components/sections/HeroSection.tsx
    - src/components/sections/TwoModesSection.tsx
    - src/components/shared/RevealOnScroll.tsx
    - src/lib/motion.ts
  </read_first>
  <action>
    `scripts/verify-motion-foundation.mjs`를 새로 만들고 Node 표준 라이브러리만 사용해 foundation guardrail을 검증한다. 스크립트는 실패 시 `process.exit(1)` 해야 한다. QUAL-01과 D-03/D-04를 회귀 없이 잠그기 위해 검사 항목은 고정한다. 첫째, `src/components/shared/RevealOnScroll.tsx` 첫 줄에 `'use client';`가 있어야 한다. 둘째, `src/lib/motion.ts`에 `fadeUp`, `fadeIn`, `scaleUp` export가 있어야 한다. 셋째, `HeroSection.tsx`와 `TwoModesSection.tsx`만 섹션 레이어의 client component로 남고, `BeyondScreenSection.tsx`, `SocialModeSection.tsx`, `PrivateModeSection.tsx`, `AIAgentSection.tsx`, `ApplicationSection.tsx`, `PocSection.tsx`, `Web3Section.tsx`, `PioneerSection.tsx`에는 `'use client'`가 없어야 한다. 넷째, `src/app/page.tsx`는 `Header`, `HeroSection`, `BeyondScreenSection`, `TwoModesSection`, `SocialModeSection`, `PrivateModeSection`, `AIAgentSection`, `ApplicationSection`, `PocSection`, `Web3Section`, `PioneerSection`, `Footer`를 지금 순서대로 유지하는지 검사해 foundation phase가 page assembly를 건드리지 않았음을 보장한다. `package.json`에는 `"verify:motion-foundation": "node scripts/verify-motion-foundation.mjs"`를 추가한다.
  </action>
  <acceptance_criteria>
    - `rg -n "\"verify:motion-foundation\": \"node scripts/verify-motion-foundation\\.mjs\"" package.json` 가 매치된다.
    - `rg -n "BeyondScreenSection|SocialModeSection|PrivateModeSection|AIAgentSection|ApplicationSection|PocSection|Web3Section|PioneerSection" scripts/verify-motion-foundation.mjs` 가 매치된다.
    - `rg -n "process\\.exit\\(1\\)|RevealOnScroll|fadeUp|fadeIn|scaleUp|HeroSection|TwoModesSection|Header|Footer" scripts/verify-motion-foundation.mjs` 가 매치된다.
    - `npm run verify:motion-foundation` 이 종료 코드 0으로 통과한다.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:motion-foundation</automated>
  </verify>
  <done>foundation phase 전용 검증 명령으로 reduced-motion primitive 존재와 server/client 경계 회귀를 자동으로 확인할 수 있다.</done>
</task>

</tasks>

<verification>
필수 자동 검증 순서:
1. `npm run verify:motion-foundation`
2. `npm run lint`
3. `npm run build`
</verification>

<success_criteria>
- `src/lib/motion.ts`가 restrained preset과 공통 timing constant를 제공한다.
- `src/components/shared/RevealOnScroll.tsx`가 D-03 최소 API와 reduced motion fallback을 중앙집중식으로 제공한다.
- 기존 서버 섹션 파일들은 `'use client'` 없이 유지되고, 이 조건을 `npm run verify:motion-foundation`으로 반복 검증할 수 있다.
</success_criteria>

<output>
After completion, create `.planning/phases/01-motion-foundation/01-01-SUMMARY.md`
</output>
