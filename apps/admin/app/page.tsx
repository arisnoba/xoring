export default function AdminRuntimeHomePage() {
  return (
    <main>
      <h1>XORing Admin Runtime</h1>
      <p>
        This standalone Next.js runtime exists to serve the routed admin surface
        outside the public static export.
      </p>
    </main>
  );
}
